function result = MedianFilter(img)
[height, width] = size(img);
result = zeros(height, width);
List = zeros(9,1);
for i = 1: height -1
    for j = 1 : width-1
        index = 1;
        for p = i - 1 : i + 1
            for q = j - 1: j+1
                List(
                index = index+1;
            end
        end
    end
end
